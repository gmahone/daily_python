def split_and_merge(string_, separator):
    words = string_.split(" ")
    paste([separator.join(list(c)) for c in word])        
    ##split_string = list(string_)
    ##print(separator.join(split_string))
    pass
